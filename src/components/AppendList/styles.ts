import styled from "styled-components";

const StyledAppendList = styled.ul``;

const EmptyLine = styled.ul`
  margin: 80px 0 80px 0;
  text-align: center;
  font-weight: 400;
  font-size: 20px;
`;

export { StyledAppendList, EmptyLine };
