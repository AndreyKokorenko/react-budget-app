import styled from "styled-components";

const StyledInput = styled.input`
  padding: 0 0 0 20px;
  max-width: 200px;

  font-weight: 500;
  font-size: 20px;

  background-color: transparent;
`;

export { StyledInput };
