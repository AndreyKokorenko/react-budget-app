import styled from "styled-components";

const StyledButton = styled.button`
  padding: 12px 148px;
  margin: 20px auto 0;

  font-weight: 400;
  font-size: 16px;

  background: #23c9ff;
  border-radius: 10px;
  color: white;
`;

export { StyledButton };
